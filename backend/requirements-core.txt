# Core backend runtime for auth, dashboard, integrations, upload pipeline, and parsers.
# Install this first when the full ML requirements are too heavy for pip to resolve.

fastapi>=0.120.1,<1.0.0
uvicorn[standard]>=0.31.1,<1.0.0
pydantic>=2.11.7,<3.0.0
pydantic-settings>=2.10.1,<3.0.0
python-multipart==0.0.9

# Database
sqlalchemy==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# Cache / Queue
redis==5.0.4
celery==5.4.0

# Parsing / matching
lxml==5.2.1
rapidfuzz>=3.9.7,<4.0.0

# Auth
pyjwt>=2.10.1,<3.0.0
passlib[bcrypt]==1.7.4
